# Project
Changes comes before the rules
These are the three rules 
1. Add files to staging: git add filename
2. Commit: The things we changed we make into a commit : git commit -m "message"
3. push:  git push